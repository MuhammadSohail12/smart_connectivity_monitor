name: smart_connectivity_monitor_example
description: Full demo app for smart_connectivity_monitor package.
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  smart_connectivity_monitor:
    path: ../
